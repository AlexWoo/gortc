buildlist="gortc gortc-test"

CURDIR=`pwd`
OLDGOPATH="$GOPATH"
export GOPATH="$CURDIR"

for def in $DEPENDENCE
do
    echo "go get $def"
    go get $def
done

gofmt -w src
for p in $buildlist
do
    go build $p
done

export GOPATH="$OLDGOPATH"
